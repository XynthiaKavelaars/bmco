% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_functions.R
\name{bmvb}
\alias{bmvb}
\title{Bayesian Multivariate Bernoulli Test}
\usage{
bmvb(
  data,
  grp,
  grp_a,
  grp_b,
  y_vars,
  test = c("right_sided", "left_sided"),
  rule = c("All", "Any", "Comp"),
  w = NULL,
  prior_a = 0.5,
  prior_b = 0.5,
  n_it = 10000,
  return_samples = FALSE
)
}
\arguments{
\item{data}{Data frame containing the data.}

\item{grp}{Character string. Name of the grouping variable.}

\item{grp_a}{Value of grp indicating first group.}

\item{grp_b}{Value of grp indicating second group.}

\item{y_vars}{Character vector. Names of outcome variables (currently supports 2 outcomes).}

\item{test}{Character. Direction of test: "left_sided" for P(A>B) or "right_sided" for P(B>A).
Default is "right_sided".}

\item{rule}{Character. Decision rule: "All" (all outcomes favor hypothesis),
"Any" (at least one outcome favors hypothesis), or "Comp" (weighted combination).
Default is "All".}

\item{w}{Numeric vector. Weights for compensatory rule. Only used if rule = "Comp".
If NULL and rule = "Comp", equal weights are used. Default is NULL.}

\item{prior_a}{Numeric. Prior hyperparameter (Dirichlet) for group A. Default is 0.5 (Jeffreys' prior)}

\item{prior_b}{Numeric. Prior hyperparameter (Dirichlet) for group B. Default is 0.5 (Jeffreys' prior).}

\item{n_it}{Integer. Number of MCMC iterations. Default is 10000.}

\item{return_samples}{Logical. Should posterior samples be returned? Default is FALSE.}
}
\value{
A list of class "bmvb" containing:
\describe{
\item{estimates}{Posterior means and standard deviations for each group}
\item{sample_sizes}{Sample sizes for each group}
\item{delta}{Treatment effect estimates and posterior probability}
\item{info}{Test specifications}
\item{samples}{Posterior samples (if return_samples = TRUE)}
}
}
\description{
Perform a Bayesian test for differences between two groups on multiple binary outcomes
using a Multivariate Bernoulli distribution.
}
\examples{
\dontrun{
# Example with simulated data
result <- bmvb(
  data = my_data,
  grp = "treatment",
  grp_a = "control",
  grp_b = "experimental",
  y_vars = c("outcome1", "outcome2"),
  test = "right_sided",
  rule = "All"
)
print(result)
}
}
