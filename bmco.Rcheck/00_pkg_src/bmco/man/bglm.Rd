% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_functions.R
\name{bglm}
\alias{bglm}
\title{Bayesian Generalized Linear Model}
\usage{
bglm(
  data,
  grp,
  grp_a,
  grp_b,
  x_var,
  y_vars,
  x_method = c("Empirical", "Analytical", "Value"),
  x_def = c(-Inf, Inf),
  test = c("right_sided", "left_sided"),
  rule = c("All", "Any", "Comp"),
  w = NULL,
  b_mu0 = NULL,
  b_sigma0 = NULL,
  n_burn = 10000,
  n_it = 20000,
  n_thin = 1,
  n_chain = 2,
  start = c(0.5, 1),
  return_diagnostics = TRUE,
  return_diagnostic_plots = FALSE,
  return_samples = FALSE
)
}
\arguments{
\item{data}{Data frame containing the data.}

\item{grp}{Character string. Name of the grouping variable (will be treated as factor).}

\item{grp_a}{Value of grp indicating first group.}

\item{grp_b}{Value of grp indicating second group.}

\item{x_var}{Character string. Name of covariate variable (currently supports single continuous or binary covariate)}

\item{y_vars}{Character vector. Names of outcome variables (currently supports 2 outcomes).}

\item{x_method}{Character. Method for handling covariate: "Analytical" (numerical integration),
"Empirical" (empirical marginalization), or "Value" (specific value). Default is "Empirical".}

\item{x_def}{Numeric vector. Defines subpopulation: length-2 vector c(lower, upper) for
"Analytical"/"Empirical", or scalar for "Value". Default is c(-Inf, Inf).}

\item{test}{Character. Direction of test: "left_sided" for P(A>B) or "right_sided" for P(B>A).
Default is "right_sided".}

\item{rule}{Character. Decision rule: "All" (all outcomes favor hypothesis),
"Any" (at least one outcome favors hypothesis), or "Comp" (weighted combination).
Default is "All".}

\item{w}{Numeric vector. Weights for compensatory rule. Only used if rule = "Comp".
If NULL and rule = "Comp", equal weights are used. Default is NULL.}

\item{b_mu0}{Vector of prior means of fixed regression coefficients. Default is \code{rep(0, P)}, where P refers to the number of columns in the model matrix.}

\item{b_sigma0}{Prior covariance matrix (PxP) of regression coefficients. Default is \code{diag(1e-2, P)}, where P refers to the number of columns in the model matrix.}

\item{n_burn}{Integer. Number of burn-in iterations. Default is 10000.}

\item{n_it}{Integer. Number of MCMC iterations. Default is 20000.}

\item{n_thin}{Integer. Thinning interval. Default is 1.}

\item{n_chain}{Integer. Number of MCMC chains to be sampled. Default is 2.}

\item{start}{Numeric vector. Starting values for chains. Should have length \code{n_chain}. Default is c(0.5, 1).}

\item{return_diagnostics}{Logical. Return MCMC diagnostics? Default is TRUE.}

\item{return_diagnostic_plots}{Logical. Should MCMC chains for diagnostic plots (traceplots, autocorrelation, density) be returned? Default is \code{FALSE}. If \code{TRUE}, diagnostics are returned by default.}

\item{return_samples}{Logical. Should posterior samples be returned? Default is FALSE.}
}
\value{
An object of class \code{bglm}, a list containing:
\describe{
\item{estimates}{A list with posterior means and standard deviations of
group probabilities (\code{mean_a}, \code{mean_b}, \code{sd_a}, \code{sd_b}),
as well as posterior means (\code{b}) and standard deviations (\code{b_sd})
of the regression coefficients.}
\item{sample_sizes}{A list with group sample sizes (\code{n_a}, \code{n_b}).}
\item{delta}{A list with posterior mean differences (\code{mean_delta}),
posterior standard errors (\code{se_delta}), posterior probability of the
hypothesis (\code{pop}), and, if \code{rule = "Comp"}, the weighted
difference (\code{w_delta}).}
\item{info}{A list with prior specifications, test settings, group labels,
covariate handling method, and subpopulation definition.}
\item{diags}{If diagnostics are requested, a list with MCMC diagnostic
results for the regression coefficients.}
\item{samples}{If \code{return_samples = TRUE}, a list containing posterior
draws of \code{theta_a}, \code{theta_b}, \code{delta}, and regression
coefficients.}
}
}
\description{
Perform a Bayesian test for differences between two (sub)groups on multiple binary outcomes
using multinomial logistic regression as described in \insertCite{Kavelaars2024;textual}{bmco}.
}
\examples{
\donttest{
# Example with simulated data
# Generate data
set.seed(123)
n <- 100

data <- data.frame(
 group = rep(c("A", "B"), each = n/2),
 x = rnorm(n),
 stringsAsFactors = FALSE
)

p1 <- p2 <- rep(NA, n)

for (i in 1:n) {
 grpB <- ifelse(data$group[i] == "B", 1, 0)

 p1[i] <- plogis(-0.50 + 0.75 * grpB + 0.10 * data$x[i] + 0.20 * grpB * data$x[i])
 p2[i] <- plogis(-0.50 + 0.80 * grpB + 0.05 * data$x[i] + 0.15 * grpB * data$x[i])

 data$y1[i] <- rbinom(1, 1, p1[i])
 data$y2[i] <- rbinom(1, 1, p2[i])
}

# Analyze
result <- bglm(
 data = data,
 grp = "group",
 grp_a = "A",
 grp_b = "B",
 x_var = "x",
 y_vars = c("y1", "y2"),
 x_method = "Empirical",
 x_def = c(-Inf, Inf),
 test = "right_sided",
 rule = "All",
 n_burn = 100, # Too low for proper MCMC sampling
 n_it = 500 # Too low for proper MCMC sampling
)

print(result)
}
}
\references{
{
\insertRef{Kavelaars2024}{bmco}
}
}
